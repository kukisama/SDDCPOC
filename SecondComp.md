# 第二台计算节点

完成`第一台计算节点`的创建后，可以开始创建`第二台计算节点`。创建一台虚拟机，进行如下配置。

第二台计算节点与第一台类似，用来被VMM纳管。在后续部署中，POC-COMP2将与POC-COMP1共同承载SDN基础架构虚拟机（如网络控制器、SLB MUX等），提供分布式部署的能力。

| 虚拟机主机名称 | 功能 | IP | 掩码 | DNS | 网关 | CPU | 内存 | 硬盘 |
|----------------|------|-----|------|------|------|-----|------|------|
| POC-COMP2 | 计算节点2 | 192.148.0.10 | 255.255.255.0 | 192.148.0.2 | 192.148.0.1 | 8 | 20G | 需要嵌套放置虚拟机，至少分配200G磁盘 |

## 部署

部署过程与`第一台计算节点`基本一致。

1. 使用VHDX模板创建一台新的虚拟机，命名为`POC-COMP2`
2. 根据上表配置IP地址、DNS和网关
3. 配置嵌套虚拟化，在Hyper-V主机上执行：

```powershell
Set-VMProcessor -VMName "POC-COMP2" -ExposeVirtualizationExtensions $true
Get-VMNetworkAdapter -VMName "POC-COMP2" | Set-VMNetworkAdapter -MacAddressSpoofing On
```

4. 将POC-COMP2加入`contoso.com`域，具体操作请参考`计算机加域`章节

## 配置本地管理员

将`computeradmin`配置为`POC-COMP2`的本地管理员。操作请参考`账号体系约定`章节。

## VMM纳管计算节点

纳管过程与第一台计算节点完全一致：

1. 打开`Virtual Machine Manager 控制台`
2. 在`VM和服务`中，于`所有主机`上右键点击，选择`添加Hyper-V主机和群集`
3. 选择已创建的`运行方式帐户`
4. 在`计算机名`中输入`POC-COMP2`
5. 勾选目标主机，完成纳管向导

> 注意：如果在第一台计算节点纳管时已经创建了`运行方式帐户`，此处可以直接复用，无需重新创建。

## 检查

纳管成功后，在VMM控制台的`所有主机`下应能看到`POC-COMP1`和`POC-COMP2`两台计算节点。

确认两台计算节点均处于`正常`状态，且无严重报错。

## 课后习题

- 思考一下，POC环境中使用两台计算节点的原因是什么？如果只有一台计算节点，SDN部署会受到什么影响？
- 了解一下VMM中`主机组`的概念，尝试为两台计算节点创建一个专用的主机组。

